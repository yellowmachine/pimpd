version: '3.7'

services:
  snapclient:
    build:
      context: .
      dockerfile: Dockerfile.snapclient
    command: snapclient -h 192.168.1.57 --latency=${LATENCY}
    restart: unless-stopped
    devices:
      - /dev/snd:/dev/snd
